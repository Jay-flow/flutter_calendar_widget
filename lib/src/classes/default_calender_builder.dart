import 'package:flutter_calendar_widget/src/classes/calender_builder.dart';

class DefaultCalenderBuilder extends CalenderBuilder {
  DefaultCalenderBuilder({
    double width = 30,
    double height = 50,
  }) : super(defaultWidth: width, defaultHeight: height);
}
