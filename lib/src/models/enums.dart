/// Days of the week that the calendar.
enum DayOfWeek {
  mon,
  tue,
  wed,
  thu,
  fri,
  sat,
  sun,
}

enum CalendarSelectionMode {
  single,
  multiple,
  range,
  multipleRange,
}

enum CalendarTheme {
  dark,
  light,
}
